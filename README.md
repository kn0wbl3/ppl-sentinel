# ppl-sentinel
PPL needs to be monitored because they have terrible UI

# Helpful Postgres Commands
psql -U user -l            >> shows all databases
psql -U user -d <database> >> connects to a specific db
\dt                        >> list all tables
\du                        >> list all users

# TODO
 1) figure out if shifts update_time works
 2) turn shift_id into key from components
 2) add new table `shift_history` to add traceability
